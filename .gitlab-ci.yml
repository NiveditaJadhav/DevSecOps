image: sonarsource/sonar-scanner-cli:latest

stages:
  - build
  - test
  - sonar_analysis

sonar_analysis:
  stage: sonar_analysis
  script:
    - sonar-scanner -Dsonar.projectKey=devopsai_1 -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_261259031d20891e0ffc34a722ce32c3e5205abd
  only:
    - main
    - master
